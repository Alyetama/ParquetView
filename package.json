{
  "name": "pqviewer",
  "private": true,
  "version": "1.0.0",
  "description": "A fast, native macOS viewer for Apache Parquet files",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
