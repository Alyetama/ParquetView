{
  "name": "parquetview",
  "private": true,
  "version": "1.0.1",
  "description": "A fast, native macOS viewer for Apache Parquet files",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
